// Generic process defaults. Environment selection (conda/container) and the
// executor budget are set by the profiles in nextflow.config and by
// conf/local.config respectively.

process {
    cpus          = 16
    memory        = '16.GB'
    maxRetries    = 1
    errorStrategy = 'retry'
}
